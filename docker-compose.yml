version: '3.8'

services:
  rail-graph:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rail-graph
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      - RUST_LOG=info
